"""Deterministic quantity engine.

This package contains PURE functions only. No AI, no I/O, no randomness.
Given a typed Member, it returns auditable quantities using IS-standard
formulas. This is the half of the application that must be trusted, so it is
fully unit-tested against golden values.

See project.md section 2 ("A hard wall between AI and arithmetic") and
section 6 (formulas).
"""
